<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <style>
    body { font-family: Arial, sans-serif; font-size: 12px; padding: 20px; }
    h1, h2, h3 { margin: 0; }
    .header, .footer { text-align: center; }
    .company-info { font-weight: bold; margin-bottom: 10px; }
    table { width: 100%; border-collapse: collapse; margin-top: 10px; }
    th, td { border: 1px solid #000; padding: 4px; text-align: center; }
    .no-border { border: none; }
    .totals td { text-align: right; }
  </style>
</head>
<body>
  <div class="header">
    <h2>Bhavya Enterprises</h2>
    <p>Regd Office: SCO 393 2nd Floor, Sector 37-D, Chandigarh 160036<br />
    B.O. : Plot No. 1025, Rani Sati Nagar, Nirman Nagar, Jaipur-302019<br />
    Helpdesk: +91 9988818489, +91 9815922428<br />
    GST NO- 04AXJPV5221C1ZP</p>
    <h3>QUOTATION PROFORMA</h3>
  </div>

  <p><strong>Quotation No:</strong> <%= quotationId %> &nbsp;&nbsp; <strong>Date:</strong> <%= date %><br />
     <strong>Valid Until:</strong> <%= validUntil %> &nbsp;&nbsp; <strong>Client:</strong> <%= name %>
  </p>

  <table>
    <thead>
      <tr>
        <th>S.No</th>
        <th>Goods Description</th>
        <th>RC-SNo</th>
        <th>Make (Bank)</th>
        <th>Where in Use</th>
        <th>Unit</th>
        <th>Qty</th>
        <th>Unit Price</th>
        <th>Total Value</th>
      </tr>
    </thead>
    <tbody>
      <% rateCards.forEach((item, index) => { %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= item.description %></td>
          <td><%= item.srNo %></td>
          <td><%= item.bankName %></td>
          <td>--</td>
          <td><%= item.unit %></td>
          <td>1</td>
          <td><%= item.rate.toFixed(2) %></td>
          <td><%= item.rate.toFixed(2) %></td>
        </tr>
      <% }) %>
    </tbody>
  </table>

  <table class="totals">
    <tr><td class="no-border" colspan="8">Subtotal</td><td><%= subtotal.toFixed(2) %></td></tr>
    <tr><td class="no-border" colspan="8">GST (18%)</td><td><%= gst.toFixed(2) %></td></tr>
    <tr><td class="no-border" colspan="8"><strong>Grand Total</strong></td><td><strong><%= grandTotal.toFixed(2) %></strong></td></tr>
  </table>

  <p><strong>Total (in words):</strong> Rupees <%= new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR' }).format(grandTotal) %> Only</p>

  <div class="footer">
    <p>Authorized Signature</p>
    <p>Bhavya Enterprises</p>
  </div>
</body>
</html>
